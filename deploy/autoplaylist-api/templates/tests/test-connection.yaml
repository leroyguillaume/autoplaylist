apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoplaylist-api.fullname" . }}-test-connection"
  labels:
    {{- include "autoplaylist-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoplaylist-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
